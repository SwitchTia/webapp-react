import { NavLink } from "react-router-dom";

export default function Header({ nameApp }) {
  

  const headerLinks = [
    { title: "Home", path: "/" },
    { title: "Movies", path: "/moovies" },
  ];

  
  return (
    <header>
        <div></div>
    </header>
  );
}